export enum Sizes {
  XS = 'XS',
  SM = 'SM',
  MD = 'MD',
  LG = 'LG',
  XL = 'XL'
}

export enum techType{
  flask = 'flask',
  django = 'django',
  express = 'express',
}

export enum techUse{
  services = 'services',
  webApp = 'app_web',
}
