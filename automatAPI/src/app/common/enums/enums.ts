export enum Sizes {
  XS = 'XS',
  SM = 'SM',
  MD = 'MD',
  LG = 'LG',
  XL = 'XL',
}

export enum techType {
  flask = 'flask',
  django = 'django',
  express = 'express',
}

export enum techUse {
  services = 'services',
  webApp = 'app_web',
}

export enum configFileTypes {
  dev = 'dev',
  prod = 'prod',
}

export enum dataBaseTypes {
  postgres = 'postgresql',
  mysql = 'mysql',
  sqlite = 'sqlite',
  oracle = 'oracle',
  mssql = 'mssql',
}
