{
  "name": "{{cookiecutter.app_name}}",
  "version": "1.0.0",
  "description": "",
  "main": "{{cookiecutter.app_name}}.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node {{cookiecutter.app_name}}.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    {%- if cookiecutter.cors %}
    "cors": "^2.8.5",
    {%- endif %}
    {%- if cookiecutter.connect_DB %}
    "sequelize": "^6.28.0",
    {%- endif %}
    "express": "^4.18.2"
  }
}
